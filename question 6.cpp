#include <iostream>
#include <math.h>

using namespace std;

int main()
{ double x;
  cout<<"enter the number whose square root is to be found"<<endl;
  cin>>x;
  double z=pow(x,0.5);
  cout <<"the required number is="<<z<<endl;
  return 0;
}
